# Minimal dependencies for Streamlit Cloud
# Uses Hugging Face Inference API (no local model execution needed)
# Removed torch, transformers (not needed - using API instead)
streamlit==1.28.1
huggingface-hub==0.19.4
python-dotenv==1.0.0
pandas==2.0.3
scikit-learn==1.3.2
requests==2.31.0
numpy==1.24.3
